baseURL = 'https://riccioproductions.github.io/'
languageCode = 'it'
defaultContentLanguace = 'it'
title = 'Riccio Productions'
theme = 'gallery'
timeout = "600s"

[params]
  # La documentazione dice che defaultTheme è supportato
  defaultTheme = "dark" 
  logo = "logo.png"
  # Prova tutte le varianti comuni dei temi Hugo
  favicon = "/favicon.ico"
  icon = "/favicon.ico"
  custom_favicon = "/favicon.ico"
  # Se il tema supporta un logo per la dark mode, metti lo stesso o uno diverso
  logo_dark = "logo.png"

  # SEZIONE CRITICA: La documentazione richiede questa esatta struttura per le icone
[params.socialIcons]
   instagram = "https://www.instagram.com/riccio.productions"
   email = "mailto:riccio.prod@gmail.com"
	
[build]
  writeStats = true

[[cascade]]
  [cascade.build]
    publishResources = false